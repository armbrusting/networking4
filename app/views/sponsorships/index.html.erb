<h1>Listing sponsorships</h1>

<table>
  <tr>
    <th>Event</th>
    <th>Company</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @sponsorships.each do |sponsorship| %>
  <tr>
    <td><%= sponsorship.event %></td>
    <td><%= sponsorship.company %></td>
    <td><%= link_to 'Show', sponsorship %></td>
    <td><%= link_to 'Edit', edit_sponsorship_path(sponsorship) %></td>
    <td><%= link_to 'Destroy', sponsorship, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Sponsorship', new_sponsorship_path %>
